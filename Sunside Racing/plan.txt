SUNSIDE RACING GAME PLAN

Status
- The asset pack, first playable world/driving slice, player saving,
  seeded, disk-cached world, world scenery dressing, distinct racing
  centers, and rudimentary traffic are built. Races are not built.
- This file preserves decisions; it is not permission to implement. A future
  game build still needs the user's direct "build" or "building" instruction.

Agreed world
- Top-down, open-world racing game on a 64 x 64 grid of scene-sized sectors.
- Load nearby sectors as the player drives. Keep distant sectors as lightweight
  map data; only nearby scenery, traffic, and collisions need to be active.
- Mainland: city center; jungle northwest; desert northeast; snow southwest;
  rural dirt region southeast. Beach borders the land, with sea extending to
  the world boundary. The player cannot leave the world or drive into the sea.
- A separate island sits offshore within the 64 x 64 grid and unlocks after
  all mainland races. The current world generator places it around sector
  (56, 31); its unlock and access mechanic remain open.
- The current prototype uses 512-pixel sectors, each containing an 8 x 8 grid
  of 64-pixel terrain tiles. Only camera-near sectors are generated; explored
  sectors are saved in world.json and loaded from disk on later launches.

Roads and scenery
- City: wide grid roads, buildings, and rudimentary traffic.
- Jungle and desert: no roads, fully off-road.
- Snow: winding icy roads. Rural: branching dirt roads. Beach: off-road.
- Road connections and scenery should line up across sector boundaries.

Racing progression
- One racing center in each mainland region: city, jungle, desert, snow,
  rural. Each center has 10 races with increasing difficulty (50 total).
- Built: see "Racing centers and Elite Island completed". Center races and
  drag races both load their own track levels.
- Race routes, opponents, rewards, and precise unlock rules beyond the island
  requirement remain to be designed.

Saving completed
- On normal exit, main.py saves the car's position and heading to player.json
  in the project root. On the next launch, it resumes there, stationary.
  Earlier saves in the operating system's user data directory can be loaded
  once and are written to the new project-root location on exit.
- player_save.py writes versioned JSON atomically. It validates finite
  coordinates, drivable terrain, and solid scenery on load; an absent or
  invalid save falls back to the starting position.
- world_save.py writes project-root world.json atomically. It stores the
  explicit world seed (default 2026) and generated sprites for only explored
  sectors. An existing world.json supplies the seed and those sectors on
  launch; unexplored sectors remain lazily generated from that seed.
- Traffic and other dynamic world state are not serialized yet.
- When car selection, currency, upgrades, and race results are built, extend
  the player save with those player-owned fields.

Assets completed
- Five PNG atlases and editable bitmap JSON sources are in assets/ and bitmap/:
  terrain, roads, vehicles, structures, and props (136 named sprites total).
- assets/atlas-manifest.json records atlas grids and shader tile coordinates.
- asset_sources.py is the deterministic source for the bitmap specifications.

First playable slice completed
- main.py runs a textured 64 x 64 world with camera follow and nearby sector
  generation. It displays the city, surrounding biomes, beach, sea, and island.
- The OpenGL window requests vsync, and vsync alone paces the game loop (see
  the tearing fix below).
- The player drives with WASD or arrows; Space applies a handbrake, R unsticks
  the car nearby, and Escape opens the pause menu. Grip and top speed vary by terrain. Sea, world bounds,
  buildings, and solid props block the car.
- This slice does not include traffic, race events, opponents, menus,
  or a way to reach the locked island. Those remain future work.

Tearing / frame pacing fix completed
- Diagnosed 2026-09-25 on an Apple M3 built-in 60 Hz display (pygame-ce
  2.5.7, SDL 2.32.10). Vsync works: flip() alone paces at 16.67 ms. The old
  extra clock.tick(60) cap fought vsync and dropped frames (33 ms spikes).
  Fractional camera positions made NEAREST-filtered tiles shimmer.
- main.py now uses clock.tick() (uncapped; vsync'd flip() paces frames, dt is
  still clamped to 0.05 s) and camera_position() rounds to whole pixels.
  TARGET_FPS was removed.
- Verified with a scripted 6-second drive: 16.67 ms mean, 0.29 ms stdev,
  18 ms worst frame, no frames over 20 ms; camera always on whole pixels.
- Remaining: a visual playtest. If tile seams still show, inset atlas UVs by
  half a texel in a game-local copy of the texture shader (shaders/ is shared
  and read-only). On a display faster than 60 Hz the game will run at that
  refresh rate; dt-based movement already handles this.

World dressing completed (2026-09-25)
- world.py now places the unused scenery assets deterministically per sector:
  - Coasts: shore_north/south/east/west on beach tiles facing the sea,
    sea_foam on sea tiles touching land, occasional buoys in coastal water.
  - City: crosswalks on the four road tiles around each intersection (rotated
    for east-west arms), street lamps and traffic lights at the corners plus
    mid-block lamps, and about 1 in 5 building lots replaced by a parking_lot
    with parked traffic cars (solid). The city racing center now takes a
    building lot instead of sitting on the intersection. Street poles are
    non-solid so cornering never snags.
  - Rural: about half of rural sectors get a farmstead clear of dirt roads:
    rural_barn on a dirt_gravel yard, a 3 x 2 farm_rows field fenced with
    farm_fence, and hay_a/hay_b bales.
  - Snow: snow_marker poles (non-solid) beside straight ice roads.
  - Ferry: beach_ferry_dock on the mainland beach at (47, 31) facing east and
    on the island beach at (52, 31) facing west, with channel buoys between.
    This is scenery only; the island access mechanic is still open.
  - Variety: jungle_tree_d, pine_d, cactus_c, rock_snow_b, hay_b, bush_dry,
    palm_c added to scatter; denser scatter and 64/72/80 px size variation.
- world_save.py records GENERATOR_VERSION (now 2). World saves from an older
  generator keep their seed but regenerate cached sectors lazily.
- Vehicle-atlas render capacity raised to 512 for parked cars.
- Still unused by design: race_* props, barrier, cone, city_start/finish,
  rural_start (race events); moving traffic and racers (traffic system);
  dark_asphalt, city_parking road tile, ice_tee/dirt_tee.
- Known: at beach corner tiles only one shoreline strip is drawn; the snow
  racing center sits at the end of its icy road (pre-existing).

Racing centers, speed, and traffic completed (2026-09-25)
- Racing centers: 160 px building (solid 104) on a 4 x 4 dark_asphalt plaza
  with a race_start gate on the south edge, race_finish gate on the north,
  solid barriers flanking open entries on every side, and corner cones.
  Scatter and city street poles are kept off the plaza. World generator
  version is now 3, so cached sectors regenerate.
- Guidance: hud.py draws a yellow screen-edge arrow (gl_utils polygon helpers
  with the shared line shaders) toward the current region's center while it
  is off screen; beaches point to the nearest center; no arrow at sea. The
  window caption shows the center name, distance (10 px = 1 m), and compass
  direction.
- Car top speeds lowered about 22% (city 340, island 310, rural 280,
  desert 265, snow 255, jungle 220, beach 210 px/s). Acceleration unchanged.
- Top speeds halved again for control (2026-09-25): city 170, island 155,
  rural 140, desert 132, snow 128, jungle 110, beach 105 px/s; reverse cap
  80 x grip. Acceleration and steering unchanged. Note: city traffic
  (140-180 px/s) is now about as fast as the player; open question whether
  to slow it too.
- Traffic (traffic.py), deterministic from the world seed (56 cars at
  first; see the busy city section for the current total):
  - City: 10 rectangular loops on the road grid, 3 cars each, 140-180 px/s.
  - Snow: 4 cars there-and-back on the ice road (100-120 px/s).
  - Rural: 4 cars on the main dirt road, 1 on each branch (120-140 px/s).
  - Jungle and desert: 3 off-road 4x4s each on wandering loops kept inside
    their region (85-110 px/s).
  - Extra pairs (added 2026-09-25, reusing existing sprites): 2 taxis,
    2 buses, 2 police cars, and 2 delivery vans, each pair on its own city
    loop, plus 2 SUVs on the snow road. They are generated after the
    original 46, so the original routes are unchanged.
  - Right-hand lanes with mitered corners and U-turns at dead ends.
  - Cars are solid to the player, stop short of the player, and queue
    behind a same-direction car ahead. Yielding runs only within 1600 px of
    the player.
- Known limits: cars crossing at intersections pass through each other, and
  off-road 4x4s drive through trees and rocks. Traffic is not saved.
- Tests: test_traffic.py covers lanes, road adherence, off-road regions, and
  yielding.

Nearby respawn and pause menu completed (2026-09-25)
- R now calls Car.respawn_nearby(): it searches outward in 8 px rings (up to
  480 px) for the closest spot where the car plus 16 px of clearance is
  drivable and free of scenery and traffic, keeps the heading, and stops the
  car. On a spot that is already clear it just stops the car. It returns to
  the start only if nothing nearby is free. Typical cost is 4-10 ms.
- pause_menu.py: Escape (or losing window focus) pauses the simulation and
  draws a dimmed world, a framed panel with a PAUSED title, and Resume / Exit
  buttons. W/S or arrows choose, Enter or Space confirms, Escape resumes, and
  mouse hover and click work via check_mouse_collisions. Exit saves and quits
  like closing the window. Drawn with the rect and tex gl_utils pipelines.
  Labels come from an in-memory text atlas made with pygame fonts and tinted
  per state.
- Tests: test_respawn_and_menu.py.

Parked-car commuters completed (2026-09-25)
- parking.py: in city lots within 2 sectors of the player, about 35% of
  nose-in parked cars (chosen stably per stall) are commuters. After 3-25 s
  (then 15-45 s between trips) one backs out of its stall to the aisle,
  drives to the nearest north-south road, laps one city block in the
  right-hand lane (away from the lot first so it returns straight), comes
  back, and pulls forward into the same stall.
- While a car is out, its stall is empty and not solid; the moving car is a
  TrafficCar (same sprite, 56 px, 18 x 38 solid). It is solid to the player
  and yields like other traffic. TrafficCar gained one-way trips,
  reverse-driven segments, and per-car size and solid box.
- Trips already underway finish even if the player drives off; parked timers
  reset for lots the player has left. Nothing new is saved: static parked
  sprites stay in world.json, so every car is parked on relaunch.
- Tests: test_parking.py (including every commuter route avoiding buildings
  and centers). Parking and traffic updates cost about 0.26 ms per frame.
- Known limits: nose-out parked cars (180 degrees) never move, and
  commuters pass through other cars in the lot aisle.

Busy city traffic completed (2026-09-25)
- Target mix agreed: busy city, quiet rural and snow, very quiet jungle and
  desert. 70 extra city loops (EXTRA_CITY_LOOPS, 3 cars each) are generated
  after all earlier traffic, so earlier routes are unchanged. Rural (6),
  snow (6), jungle (3), and desert (3) are unchanged. Total 266 cars: 248 in
  the city. Plus parked commuters.
- Measured: about 9 cars on screen in the city on average (max about 18).
  Traffic and parking updates cost about 0.9 ms per frame.
- Intersection yielding: a car about to enter a city intersection box
  (60 x 60 px) waits if a perpendicular car is inside it or claimed it
  first. Cars already inside never stop for the crossing.
- Queues behind the player or a car ahead always hold, so cars never drive
  through each other in a queue, and a player stopped in a lane backs up
  traffic. Only intersection waits give up after 4 s (PATIENCE), which
  prevents gridlock; this happened about 5 times per simulated minute. A
  stress run (8 simulated minutes, 4 busy spots) had no car stall 10 s.
- Tests: test_traffic.py adds the regional mix, intersection yielding, and a
  no-gridlock check.

HUD, title, handling, and help completed (2026-09-25)
- The game is named Sunside Racing: the project directory is now
  "Sunside Racing/" (the marker moved with it) and the window title is a
  fixed "Sunside Racing". player_save.py keeps its legacy APP_NAME only to
  find older saves.
- HUD (hud.Hud) replaces the dynamic window title:
  - Top-left panel: region name and "<Region> racing center · N m DIR" (or
    "Open water").
  - Speedometer (now compact, bottom-right; see below): speed number (px/s shown as KM/H, tinted
    by speed) and a 20-segment bar. Lit segments run green, then yellow,
    then red, scaled to TOP_SPEED (170, city).
  - Text uses ui_text.py: pygame-font text uploaded with the same path as
    gl_utils.load_texture, drawn with the shared tex shader. Panels use the
    rect pipeline.
- Acceleration lowered: ACCELERATION 85, REVERSE_ACCELERATION 90, BRAKING 420
  (all times grip). The city takes 2.0 s to reach 170; braking from 170
  takes 0.42 s.
- Pause menu has Resume / Help / Exit. Help opens a CONTROLS page listing
  keys and the racing-center goal; BACK, Enter, or Esc returns to the menu.

Compact speedometer completed (2026-09-25)
- The speedometer moved to the bottom-right and shrank to one-third size:
  a 124 x 32 px panel with 24 px margins. It has a size-24 speed number, a
  size-15 KM/H label (at 13 the "/" was lost), and a 60 px, 20-segment bar
  that still runs green to red. The top-left region panel is unchanged.

Speedometer scaled up (2026-09-25)
- hud.SPEEDO_SCALE = 2 doubles the compact bottom-right speedometer to
  248 x 64 px (about two-thirds of the original 368 x 96). The number is
  font 48, KM/H is font 29, and the bar is 120 px. Change SPEEDO_SCALE to
  resize everything together.

Orbiting guide arrow completed (2026-09-25)
- The yellow racing-center arrow now circles the player's car instead of
  sitting at the screen edge. Its base is 56 px from the car's center and
  its tip 88 px, pointing at the current region's center (hud.orbit_tip).
  It follows the car even where the camera clamps at the world edge.
- It is drawn last (after the world and the HUD) so nothing covers it, and
  hidden only while the pause menu is open or within 300 px of the center.
- The arrow is longer and narrower (32 x 16 px, dark outline) so its tip
  reads clearly at any angle. This resolves the old note about the arrow
  passing under the HUD panel.

Planned: on-foot mode with pedestrians and NPCs (agreed 2026-09-25, not built)
- Idea: the player can exit the car and walk around. Pedestrians walk fixed
  paths (sidewalks, crosswalks, into and out of buildings). Select NPCs can
  be interacted with.
- Performance: fine at this scale. A frame is about 2.6 ms render plus
  0.9 ms traffic out of 16.7 ms. Packing costs about 1.4 us per on-screen
  sprite, so even 1,000 on-screen sprites add only about 1.4 ms.
  Pedestrians should be simulated only near the player (like parked
  commuters) with no all-pairs avoidance checks.
- Likely pieces:
  - New pedestrian/NPC sprite atlas (PNG skill), drawn above props and
    below vehicles.
  - Sidewalk and crosswalk walk paths; door points on buildings (a
    pedestrian "enters" by reaching a door and despawning, later
    reappearing).
  - On-foot player: small collision box, walking speed, E to exit or enter
    the car. The parked player car becomes a world obstacle.
  - NPC interaction: proximity prompt plus a dialogue panel reusing the
    pause-menu UI style.
  - Cheap wins first: throttle HUD text re-renders (about 1 ms to 0.1 ms)
    and tighten the sprite gather margin.
- Decisions (2026-09-25):
  - Camera zooms in while on foot (proposed 2x; exact level still open).
  - Buildings: doors only. Pedestrians walk to a door, disappear, and later
    reappear. Interiors only for a few buildings the user will choose later.
  - NPCs: mostly tiny missions (short fetch, deliver, or drive-to tasks),
    started by walking up to an NPC and pressing E.
  - Cars stop for pedestrians (same yield rule traffic uses for the
    player); pedestrians cannot be hit.
- Build order (steps 1 and 2 done 2026-09-25):
  1. DONE: render wins. world.visible_sprites gathers only sectors touching
     the view plus a 96 px margin (GATHER_MARGIN): 1,109 sprites gathered
     instead of 2,588, 0.37 ms to 0.17 ms. HUD text re-renders at most every
     0.2 s (speed every 0.1 s). The real HUD cost was reusing one instance
     buffer for every label in a frame (each write waited for the GPU); each
     label now has its own buffer: HUD 1.09 ms to 0.09 ms. The whole frame
     went from 2.63 ms to 1.67 ms (vsync off, city start).
  2. DONE: on foot.
     - E beside a nearly stopped car (under 15 px/s) steps out on the
       driver's side (else passenger, back, front); E within 44 px of the car
       gets back in.
     - walker.py: W/A/S/D move in screen directions at 60 px/s (Shift runs
       at 105), slide along walls, and use a 12 px collision box and
       2-frame walk animation.
     - While on foot the parked car is solid (CollisionManager.fixed) and
       traffic stops for it and for the walker (Traffic.update blockers).
     - The camera eases to 2x zoom on foot and back to 1x in the car.
       GameState.render scales positions and sizes by zoom, and the camera
       snaps to whole screen pixels.
     - HUD hides the speedometer on foot and shows a bottom-center prompt
       ("E  Get in"), reusable for NPC prompts. R unsticks the walker too
       (shared nearest_clear_spot).
     - New people-atlas.png (32 px cells: player_idle, player_walk_a,
       player_walk_b) from asset_sources.people(); a red jacket and cream cap
       with a dark red brim. Drawn above props and below vehicles.
     - The Controls help page lists Shift and E and the on-foot directions.
     - Known: a car parked in a traffic lane backs up traffic until you
       move it.
  3. DONE (2026-09-26): pedestrians, encampments, and calling the car.
     - Q on foot: the car is placed at the nearest clear spot beside the
       walker (within 320 px, not on top of them), facing the walker's way.
       A "Q  Call car" prompt shows once the car is over 320 px away.
     - people-atlas grew to 17 kinds x 3 frames (idle, walk_a, walk_b):
       player, city_a..h, farmer_a/b (straw hats, overalls), snow_a/b
       (parkas, beanies), nomad_a/b (robes, head wraps), explorer_a/b
       (safari hats, backpacks). New camp-atlas (64 px): tent_tan,
       tent_green, campfire, crate_stack, barrel_pair, bedroll_red/blue,
       log_bench. Both are generated by asset_sources.py.
     - Encampments: 5 desert and 5 jungle camp sectors (World.camps, seeded,
       spaced apart, at least 2 sectors from racing centers). Each has three
       tents facing a campfire, crates, barrels, bedrolls, and a log bench on
       trampled ground. GENERATOR_VERSION is now 4, so cached sectors
       regenerate.
     - pedestrians.py:
       - Groups exist only within 2 sectors of the player (dropped past 3).
       - City: 12 per block walk the sidewalks (half each way, 8 px apart),
         pause at corners, enter building doors (hidden 3-9 s), and cross
         to the next block at crosswalks and back.
       - Farms: 2 farmers circle each farm and use the barn door.
       - Plazas: 3-4 people mill around each racing center.
       - Camps: 2 seated on the bench facing the fire, 2 walk the camp
         ring and duck into tents.
       - Always simulated: 8 snow and 8 rural walkers on road shoulders.
     - People wait when the player stands in front of them, are solid to the
       player except when already overlapping (never trapping them), and
       traffic stops for anyone on a road tile (crosswalks).
     - Cost at the city start: a full frame (simulation plus rendering) is
       3.1 ms driving (27 people on screen) and 2.3 ms on foot. Groups take
       about 1 ms to create when crossing into a new sector row.
     - Known limits: pedestrians pass through each other; off-road 4x4s can
       drive through camps; nobody is saved (all regenerate on load).
  4. NPC prompt, dialogue panel, and a small mission system.
  5. Interiors for buildings the user picks.
- Still open: exact zoom level; which buildings get interiors; the first
  handful of missions.

Save on-foot mode and walker position completed (2026-09-25)
- player.json version 2: {"version": 2, "mode": "drive"|"walk",
  "car": {x, y, heading}, "walker": {x, y, heading} (walk mode only)}.
  Version 1 saves ({"player": car}) still load, as drive mode. The next quit
  rewrites them as version 2.
- PlayerSave.load_state(collisions) returns (car, walker); load() still
  returns just the car. The walker is validated against the world, scenery,
  and the parked car. If it is blocked or corrupt, the player steps out
  beside the car (exit_spot); if that fails, the player resumes driving. An
  invalid car or unknown mode discards the save as before.
- main.py resumes on foot with the parked car solid and the camera already
  at 2x zoom, and saves (car, walker) on exit.
- Tests: four new cases in test_player_save.py; an isolated end-to-end run
  resumed on foot, walked, quit, and saved walk mode.
- Removed note: "Quitting on foot saves the car's position; you start the
  next session in the car" no longer applies.

Missions (built 2026-09-26; design below, build notes at the end)
- Mission givers: 3 NPCs per mainland region, one per mission type
  (delivery, speed check, drag race). Only one mission can be active at a
  time. After a completion the giver offers a fresh mission.
- Mastery points come only from these missions and are the player's only
  way to upgrade. Mastery unlocks speed in regions and unlocks additional,
  harder NPCs that offer the same mission types for more mastery.
- Delivery: take a package from the giver to another NPC, possibly in
  another region. No time limit. Start at 100 points and lose points per
  crash by difficulty: Easy 5, Medium 10, Hard 15. At least 75 points gives
  2 mastery, at least 50 gives 1, below 50 gives 0.
- Speed check: reach the target before the timer runs out.
  Timer = distance / (player top speed x 0.8 x s). Success gives 1 mastery.
- Drag race: loads its own empty-road level, either a quarter-mile straight
  or a single-lap circuit, against one opponent. Win gives 3, loss gives 0.
- Difficulty scale s is random in 0.5-1.25 and not tied to the player. The
  opponent's top speed, acceleration, and cornering grip are s times the
  player's. The AI brakes before turns and accelerates on straights. The
  label is set by s.
- Failure and retry: a failed mission ends and returns the player to the
  NPC who gave it. The same mission can be retried.
- Racing centers are separate from missions: 3-lap races with opponents
  scaled by that center's progression. The island unlocks when every
  mainland center is fully complete. This replaces the earlier "50 races
  unlock the island" wording. Race-center races are a later feature.
- Proposed defaults where unanswered:
  - Crash: a collision that stops the car from above 40 px/s, with a 1 s
    grace period.
  - Labels: s < 0.8 Easy, s < 1.05 Medium, otherwise Hard.
  - Distance: straight line x 1.3 for detours, since the jungle, desert,
    and beach have no roads.
  - Drag level: straight or single-lap circuit chosen at random, themed to
    the giver's region.
  - Harder givers skew s upward (0.8-1.25).
  - Givers: placed near each region's racing center plaza, marked with a
    floating icon, with an E prompt and a mission offer panel (type,
    difficulty, reward; accept or decline).
  - Mastery saved in player.json version 3 and shown in the HUD.
- The user confirmed the proposed defaults above (2026-09-26): three
  difficulty labels, the crash rule, straight-line x 1.3 distance, givers
  near the plazas, and a random drag level type. Levels (below) replaced the
  earlier +2%-per-mastery and 10/25-mastery ideas.
- Levels (agreed 2026-09-26, supersedes per-mastery speed and the 10/25
  mastery thresholds): mastery feeds a level. Going from level n to n+1
  costs 10 x n mastery. All upgrades happen on level-up.
  - Rewards rise by 1 per level (agreed, replaces a level multiplier so
    that higher levels get harder): mission mastery = base reward +
    (level - 1) for any earning result (delivery 2/1, speed check 1, drag
    win 3). Failed or zero results stay 0. With a base of about 2 per
    mission: 5 missions for level 1 to 2, about 8 for level 5 to 6, about
    9 for level 10 to 11, approaching 10 per level.
  - Harder mission givers unlock at level 5 (agreed) and double the
    level-adjusted reward (agreed).
  - Proposed defaults: one level per region (mastery from a region's givers
    counts toward that region); +4% top speed in that region per level.
- Mission persistence (agreed 2026-09-26): player.json (version 3) stores
  per-region mastery, each giver's pending offer (type, difficulty scale,
  destination or drag level, reward) so offers survive restarts, and the
  ongoing mission. On exit an ongoing mission is saved as a queued mission:
  on the next launch it is not in progress but waits at its giver, and
  restarts from the beginning when the player accepts it again. Delivery
  points and speed-check timers reset. Only one queued or ongoing mission
  exists at a time.

Mission system build notes (2026-09-26)
- Modules:
  - progression.py: per-region level and mastery. Level n to n+1 costs
    10 x n. Reward = base + (level - 1), doubled by veterans; nothing earned
    stays 0. +4% top speed per level in that region (the car's speed_scale;
    beach, sea, and island stay at 1.0). Veterans unlock at level 5.
  - missions.py: 30 givers (delivery, speed check, drag, plus veteran
    copies) found on open, off-road ground about 200 px around each
    mainland center. Offers are seeded and persisted per giver; veterans
    roll s in 0.8-1.25. Delivery and speed-check destinations are 3,000-
    15,000 and 2,000-6,000 px away on open drivable land. The speed timer
    samples the straight line, using each region's top speed x the level
    speed scale x 0.8 x s, times 1.3.
  - drag_race.py: TrackLevel builds either a 4,032 px quarter-mile straight
    or one of three rectilinear single-lap circuits, 3 tiles wide, with
    curbs, inner apexes, barrier walls, and tire stacks, themed by the
    giver's region. It stands in for World for Car and CollisionManager.
    Rival: stats are s times the base car (not the player's upgrades, so
    levels are the player's edge). It brakes to 70 x s px/s for corners,
    reacts 0.2-0.6 s late, starts in the right lane, and is solid to the
    player. Race: 3 s countdown; first to finish wins.
  - mission_ui.py: modal offer panel (Accept / Decline) and result panel
    (success or failed, mastery, level-up, veterans unlocked).
  - HUD: top-right mission panel (points and crashes, or the timer, with a
    red crash flash) and a big center banner (countdown, GO!, win or lose).
    The guide arrow and text point at the mission target or queued giver.
  - New assets: marker-atlas (mission badges, veteran variants, drop-off
    and finish) and track-atlas (track tiles, barrier, tires).
- Flow: walk up to a giver, press E, accept. Delivery and speed checks run
  in the open world (crash = collision stopping the car above 40 px/s, 1 s
  grace). A delivery below 50 points fails immediately. The speed clock
  starts when you drive off. Drag races load their level, then return you
  on foot beside the giver with your car called nearby. Failure also
  returns you to the giver; the same offer stays for a retry. Success
  brings a fresh offer.
- Save: player.json version 3 adds "missions": progress, offer counter,
  offers per giver, and queued. An ongoing mission, even mid-race, saves
  as queued and restarts from scratch when accepted again.
- main.py is now a Game class (world and race phases, panels, input).
- Tests: test_missions.py (20). Suite total 73. End-to-end checks: offer,
  accept, save as queued, deliver with a level update, drag race won and
  returned to the giver, veterans at level 5. World frame with missions
  is about 1.8 ms.
- Balance note: a perfect player beats rivals up to s = 1.0 at level 1,
  1.1 at level 3, and 1.25 at level 7 (both straights and circuits).
- Known limits: givers and recipients are not solid; the rival does not
  avoid the player; recipients stand where the offer rolled, sometimes in
  another region's wilds; center races (3 laps) are not built yet.

Pause-menu mastery page completed (2026-09-26)
- The pause menu now has Resume / Mastery / Help / Exit. The Mastery page
  shows one row per mainland region: level, progress bar and mastery toward
  the next level, top-speed bonus, whether veteran givers are unlocked,
  missions completed by type (delivery, speed check, drag), and the
  region's ongoing or queued mission. Esc or Back returns to the button
  you came from.
- progression.Progress now tracks completed missions per region and type.
  They are saved inside "missions.progress"; older saves start at 0.
  Missions.mastery_rows() feeds the page each frame while it is open. Text
  cells are DynamicLabels with their own quad buffers, drawn with an atlas
  grid of (1, 1).
- Tests: menu navigation for four buttons and both sub-pages, and a
  mastery-rows test (levels, bonus, veterans, completions,
  ongoing/queued, save round trip). Suite total 74.

Fast travel completed (2026-09-26)
- At level 3 in a region (progression.FAST_TRAVEL_LEVEL), the pause menu's
  Mastery page shows a TRAVEL button on that region's row. Other rows show
  "Lvl 3" (locked), "Here" (you are in it), or "Busy" (a mission is
  ongoing, including drag races, so travel cannot shortcut one). Back
  starts selected, and the selection is kept when rows refresh, so Enter
  never travels by accident.
- fast_travel.destination(): follow the straight line from the player
  toward the region's racing center to the first point inside the region,
  go 192 px further in, then find the nearest clear, car-sized spot in that
  region (within 400 px, avoiding traffic, pedestrians, and scenery).
  The player lands in the car, stopped and facing into the region; walking
  ends and the camera returns to driving zoom. If no spot is clear, a
  message panel says so.
- Tests: travel status per region, landing spots in all four other regions
  (in-region, clear, near the border), menu travel buttons, and selection
  surviving row refreshes. Suite total 77. End-to-end: menu to TRAVEL to
  landing 467 m away in the snow region.

Mastery page layout tweak (2026-09-26)
- Columns are now REGION, LEVEL, PROGRESS, SPEED, COMPLETED, MISSION,
  TRAVEL, VETERANS, so the unlocks read in level order (fast travel at 3,
  veterans at 5). Locked veterans show "Lvl 5", matching TRAVEL's "Lvl 3".
  The column indices are named constants (TRAVEL_COL, VETERANS_COL).

Matching fonts for TRAVEL and VETERANS (built 2026-09-26)
- The TRAVEL cell is bold ("Lvl 3", "Here", "Busy", "TRAVEL") but the
  VETERANS cell is regular ("Lvl 5", "Unlocked"). Fix: make the VETERANS
  cell bold too, adding VETERANS_COL to the bold set in PauseMenu's cell
  setup. Same size (22) and alignment rules; colors unchanged. Done and
  checked in a screenshot.

Autosave completed (2026-09-26)
- autosave.Autosave: saves every 60 s (AUTOSAVE_INTERVAL) and right after
  every mission result (delivery, speed check, drag race, including
  level-ups). The timer runs even while paused; saves are held during drag
  races, which save when they end. A "Saved" note shows top-center for
  1.5 s.
- Game._autosave(): player.json is written synchronously (about 0.3 ms).
  world.json is snapshotted on the game thread (shallow copy of the
  sector table; sector lists are never mutated after put) and encoded and
  written on a worker thread, still atomically (temp file, fsync,
  replace). One write at a time; a request while one runs is skipped. A
  failed write marks the store dirty again. The exit save waits for any
  running write, then writes synchronously. World writes now use one
  json.dumps + write instead of streaming json.dump.
- Measured: a synchronous world save of 328 sectors took 113 ms; the
  autosave frame now takes about 2.7 ms.
- Tests: test_autosave.py (timing, background write reloads cleanly,
  sectors explored during a write are kept). Suite total 80. End-to-end:
  interval save at 60 s rewrote both files; a completed mission saved at
  once.
- Still possible: a crash loses at most about 60 s of exploring. Mission
  results are saved immediately.

Racing centers and Elite Island completed (2026-09-26)
- User confirmed: center races load separate track levels (not open-world
  routes), and the island needs every center complete plus level 25 in a
  region (implemented as: the best region level >= ISLAND_LEVEL).
- Region track surfaces (track-atlas, track_<region>_<part>): city asphalt,
  snow ice, rural mud, desert sand, jungle grass. Races (and mission drag
  races) drive with that region's grip and top speed from car.SURFACES,
  plus the player's level speed bonus there. Runoff uses OFF_SURFACE.
- racers.py: one story line per region and 10 named rivals each (name,
  line, car). Race 10 is the champion (Queen Lux, The Warden, Sultan Rae,
  The Glacier, Big Mae).
- Race k: 3-lap circuit on its own seed-generated track (see "Seed-generated
  race tracks"), rival calibrated so a flawless player wins at level L_k,
  with L_k = 1, 2, 3, 5, 7, 9, 11, 14, 17, 20. The
  rival scales the region's base car (grip-limited accel, brake, corner).
  Checked: a flawless player first wins race k at about level L_k in
  every region (race 10: level 20; 19 on ice).
- Flow: on foot within 130 px of a mainland center, "E  Racing center ·
  Race k/10" opens an offer (rival line, surface, suggested level vs
  yours). Blocked while a mission is active. A win records
  Progress.races[region] (saved as "races"; old saves start at 0) and shows
  "Race k+1 unlocked" or champion. A loss offers a retry. Either way you are
  placed on foot at the center with your car nearby, and it autosaves. No
  mastery from races. The HUD shows "VS <RIVAL>", place, "Lap n/3", and the
  time.
- Elite Island: Progress.island_unlocked() = all 5 centers 10/10 and a
  region at level 25. On any mainland beach, "T  Travel to Elite Island"
  lands the player in the car south of the island center; on the island,
  "T  Return to mainland" lands at the mainland ferry dock. Blocked during
  missions. Island content comes later.
- Mastery page: CENTER x/10 column (green when complete) and an island
  status line ("centers n/5 · best level n/25"). Controls page: T row; the
  E row mentions racing centers.
- Bug caught end-to-end and fixed: a new "center" variable in the prompt
  code shadowed the guide-arrow target and would have crashed near centers.
- Tests: test_centers.py (10). Suite total 90. End-to-end: center offer,
  3-lap race HUD (city and snow), win and loss results, mastery page,
  island travel there and back, saved race counts.

Seed-generated race tracks completed (2026-09-26)
- track_gen.py: a circuit is the outline of a random blob of grid cells
  (6 tiles each), grown without holes or corner-only contacts, so the
  outline is always a simple clockwise loop. Parallel stretches stay 6
  tiles apart, so barriers never overlap. The start is the longest
  eastbound straight (at least 6 tiles). Same seed, same track; generated
  on load (about 0.1 ms per layout), not cached. TRACK_GENERATOR_VERSION = 1.
- Center race k: seed "<world seed>-<region>-<k>", blob size
  racers.track_size(k) = 4 + 8k // 10 (4 to 12 cells). Average lap grows
  from about 370 m (race 1, 6 corners) to about 890 m (race 10, up to 16
  corners). All 50 layouts are unique.
- Balance: every rival speed scales with its scale, so its time is
  T(1) / scale plus reaction. drag_race.calibrated_scale() simulates the
  rival once at scale 1 (at 60 Hz) and picks the scale at which a flawless
  player at REQUIRED_LEVELS[k] wins by 0.5%. Checked for all 50: win at the
  target level, lose one level below. racers.rival_scale was removed. Race
  load including calibration is 37 ms at worst.
- Mission drag circuits: new offers get their own generated layout (offer
  seed, 4-7 cells). Old saved offers with a fixed "shape" still load.
- Tests: generator (50 unique, deterministic, simple clockwise loops that
  start eastbound, barriers never on the track), per-race balance. Suite
  green. End-to-end center flow re-run on generated tracks.

Corner cutting and ideal-line balance (2026-09-26)
- Problem found in play: a level-2 player beat a level-5 race. Tracks are
  3 tiles wide with square corners, so cutting each corner at the inside
  apex makes the lap 25-28% shorter. The calibration assumed the
  centerline, so races 4-5 were winnable at level 1.
- Fix: drag_race.best_line_length() is the ideal line (every corner cut at
  its apex, 84 px inside the centerline corner). flawless_time() now uses
  it, so "just winnable at level L" holds for a player who cuts corners.
- Rival corner cutting (racing-center 3-lap races only): at each corner the
  rival cuts to the apex with probability racers.cut_chance(k) = 10%
  (race 1) to 90% (race 10), rolled from its seed, so a race always plays
  out the same way. The rival drives a precomputed path for all laps;
  its race progress is measured on the centerline like the player's.
  Mission drag rivals never cut. On drag circuits, the offer scale s now
  means "a flawless s x stock car on the ideal line", as on straights.
- Calibration: race time is not exactly 1 / scale (acceleration and
  braking scale too), so calibrated_scale() now refines the estimate with
  up to 6 runs of the same seeded rival, to within 0.02 s. All 50 center
  races: win at the target level, lose one level below, rival always on
  track. Worst race load is about 0.5 s (race 10); the countdown covers it.
- Bugs fixed on the way: the rival froze at the lap seam (the final-segment
  check compared points, which repeat every lap; it now uses the index), and
  the calibration loop now raises instead of hanging if a rival never
  finishes.
- Tests: ideal line shorter than the centerline, cut rate rises with the
  race and the rival stays on track, drag rivals never cut and are
  balanced for the ideal line. Suite green (about 43 s).

Local rival corner cuts (built 2026-09-26)
- Problem: a cut corner is a single apex waypoint. The rival drives
  straight from the previous point to the apex and on to the next corner,
  a long diagonal across the whole straight (hugging the edge) instead
  of cutting only the corner.
- Fix: a cut corner becomes three waypoints: rejoin the centerline 96 px
  (1.5 tiles) before the corner, clip the apex, return to the centerline
  96 px after the corner. Straights are driven on the centerline; only
  the corner itself is cut.
- The rival loses some of its shortcut, so the calibration raises its
  scale to keep "win at the target level" exact (re-verify all 50 races).
  The player's ideal line and the balance target are unchanged.
- Built: CUT_REACH = 96 px entry and exit waypoints; only the apex (or an
  uncut corner) is a braking point. The rival also merges from its grid
  slot to the centerline 1 tile after the start. Verified over all 50
  center races: mid-straight distance from the centerline is 0 px, win at
  the target level and lose one level below, rival always on track. Worst
  race load is about 0.65 s. New test: cuts stay at the corners and
  straights stay central.

Diagonal rival corner cuts (built 2026-09-26)
- Problem: a cut is centerline -> apex -> centerline (three waypoints), so
  the rival turns toward the apex, turns 90 degrees there, then turns back:
  it reads as a three-sided polygon, not a corner cut.
- Fix: a cut becomes one straight diagonal (a 45-degree chamfer). Leave the
  centerline 168 px before the corner and rejoin 168 px after it; the
  diagonal between them passes exactly through the 84 px apex. Only two
  gentle 45-degree heading changes, which the rival's turn smoothing rounds
  off. Segments are at least 384 px, so two cut corners in a row still
  leave a short straight between them.
- The rival brakes where the diagonal starts. Re-run calibration and
  checks: all 50 races balanced, rival on track, straights central.
- Built: CUT_REACH = 2 x APEX = 168 px; a cut is two waypoints (enter and
  exit) whose diagonal passes exactly through the apex; the brake point is
  the entry. If the first corner's cut begins before the start merge
  point, the merge point is dropped (it caused a 145-degree double-back).
  Verified over all 50 races: sharpest path bend 90 degrees (only at uncut
  corners), mid-straight 0 px off center (beyond 3 tiles from corners),
  balanced at target level and one below, rival always on track. Tests
  updated plus a "cuts are diagonals, not hairpins" test.

Abort mission / quit race completed (2026-09-26)
- Pause menu: while an in-world mission (delivery or speed check) is
  ongoing, ABORT MISSION appears under Resume; during any race (center or
  mission drag), QUIT RACE appears instead. The panel grows and the
  buttons stay centered; the selection is kept when the option comes and
  goes (PauseMenu.set_ongoing, fed by Game._ongoing()).
- Choosing it asks to confirm (MissionPanel.show_confirm; the safe answer,
  KEEP GOING or KEEP RACING, starts selected).
- Abort mission: Missions.abort() fails it (no mastery, not counted as
  completed); the same offer stays for a retry; the player is returned to
  the giver; autosave.
- Quit race: the race is marked lost (race.quit). A center race counts as
  a loss ("You quit the race against <rival>"; races won unchanged; back
  at the center). A mission drag race also fails its mission (back at the
  giver, offer kept). Both autosave.
- Queued missions are unaffected (nothing is running to abandon).
- Tests: menu items per state, selection kept, the column stays
  centered, abort keeps the offer. End-to-end: decline keeps the mission;
  abort, center-race quit, and drag quit all work with their result
  panels.

Speed check renamed to Time Trial (2026-09-26)
- Player-facing name only: missions.TITLES["speed"] = "Time Trial" (giver
  names such as "City Time Trial", the offer panel, HUD mission title,
  prompts, confirm dialogs, and "Ongoing · Time Trial"). The Mastery page
  Completed column reads "Del · Trial · Drag". The internal key stays
  "speed", so saves, offers, and marker icons are unchanged. Older notes
  in this file that say "speed check" mean the time trial.

Bug fix: quitting a race restarted it (2026-09-26)
- Cause: in Game.handle, every key press while a panel was open cleared
  pending_confirm, including arrow keys used to move to QUIT. The final
  Enter then fell through to the still-set pending_center (never cleared
  after the race started) and started the race again.
- Fix: panel state (pending_confirm, pending_center, pending_offer) is
  consumed only when the panel actually closes (outcome not None), then
  dispatched; a stale offer can no longer be accepted.
- The earlier end-to-end check set panel selections directly and missed
  this. New test_game_flows.py drives the real keyboard path (hidden
  OpenGL window, temp copies of the saves): quit a center race and return
  to the center, Esc on the confirm keeps racing, and abort a delivery
  after moving the selection. Skipped automatically when no GL window is
  available.

Left/Right on side-by-side panel buttons (2026-09-26)
- InputHandler emits menu_left (Left or A) and menu_right (Right or D).
  MissionPanel uses them on its side-by-side buttons: Left selects the
  left button, Right the right one, stopping at the edges. Applies to
  Quit/Keep racing, Abort/Keep going, Accept/Decline offers, and other
  two-button panels. Up/Down still toggle. The intents are ignored when
  no panel is open, so steering and walking are unaffected. Covered by a
  real-key test in test_game_flows.py.

Failed missions and lost races end at once (2026-09-26)
- Bug: a lost race stayed on its level for RACE_OVER_DELAY (2 s) before
  ending, and that delay only advanced while unpaused. Pausing in that
  window left the lost race (and, for drag races, the failed mission)
  "ongoing" until the player chose QUIT RACE.
- Fix: race.result == "lose" ends the race immediately (_end_race): result
  panel, return to the center or giver, and the drag mission is failed.
  Wins still show "YOU WIN!" for 2 s. In-world failures (time trial out of
  time, delivery below 50 points) were already immediate; re-verified.
- A race that already has a result is no longer "ongoing", so QUIT RACE
  is not offered during a win's banner, and _give_up ignores finished
  races (quitting could previously have turned a win into a loss).
- Tests: a lost drag race fails its mission at once with no quit or abort
  offered; pausing during a win keeps the win. test_game_flows.py now
  resets its temporary saves before each test.

Mission system revamp: no queued missions (2026-09-26)
- There is an ongoing mission or none; the "queued" state is gone
  (Missions.queued, abandon_to_queue, the QUEUED MISSION HUD panel and
  mastery text, and "queued" in player.json). Older saves that contain
  "queued" load and ignore it.
- A giver's offer stays until it is completed, so "sending the mission back
  to the giver" is just dropping the active mission: the same offer waits.
- Abort mid-mission (pause menu): failed, back at the giver, same offer (as
  before). Quit mid-race: loss, back at the center or giver (as before).
- Quitting the game mid-mission or mid-race quits it. Game._saved_pose()
  makes the save put the player on foot at the giver (or 150 px south of
  the racing center) with the car called beside them; the ongoing mission
  or race is never written. Autosaves use the same snapshot, so a crash
  mid-mission also loads back at the giver. The live session is not
  changed by an autosave. Races won never change on a quit.
- This supersedes "Mission persistence ... saved as a queued mission" and
  any other queued-mission notes above.
- Tests: missions (never saved, offer kept, old queued saves ignored) and
  real-key game flows (quit game mid-delivery and mid-race, autosave
  mid-mission).

Drag rival cap (2026-09-26)
- User feedback: drag rivals above 1.2 were too hard. missions.MAX_DRAG_SCALE
  = 1.15. New drag offers roll 0.5-1.15 (veterans 0.8-1.15); delivery and
  time trial keep MAX_SCALE 1.25. Saved drag offers above 1.15 are lowered
  to 1.15 on load (the player's pending city drag offer went 1.227 ->
  1.15). Test added.

Offer-time difficulty for drag and time trial (2026-09-26)
- Each offer records the player's region levels when it is made
  (Offer.levels, saved in player.json). Offers saved without them adopt the
  current levels on load; hand-built offers get them on accept.
- Drag: the rival is offer.scale x the player's car at offer time
  (Missions.rival_multiplier), e.g. 1.15 at level 2 = 1.196x stock. It
  never changes afterwards, so leveling up lets the player catch it. Game
  passes this to DragRace; circuits calibrate to it, straights use it.
  (Superseded by the rating system below.)
- Time trial: the clock uses the offer-time level of every region on the
  route, so it never shrinks after leveling.
- Difficulty labels use effective_scale = scale x offer-time multiplier /
  current multiplier (deliveries keep their raw scale), so a Hard offer can
  become Medium or Easy as the player levels. The label is fixed when the
  mission is accepted.
- The player's pending city drag offer (made at city level 2, player now
  level 3) was given levels city=2: rival 1.196x, shown as Hard (1.107).
- Tests: rival frozen across level-ups and saves; labels ease; time-trial
  clock frozen; old offers adopt the current levels; a real-key flow
  accepts a level-2 offer at level 3.

Car ratings (2026-09-26)
- A car's rating is 100 at level 1 and +10 per level (progression.rating).
  Rating drives speed: every 10 points is +4% top speed over stock
  (rating_speed), so the player's own speed is unchanged.
- Drag rivals are rated offer.scale x the player's rating when offered,
  frozen: 1.15 at level 2 = 126.5 (1.106x stock), beaten from level 4
  (130). This replaced "scale x offer-time speed" (1.196x stock) and makes
  drag rivals easier. Drag difficulty labels compare ratings (rival /
  yours now). Time trials still compare top speeds.
- Center race rivals are rated at their REQUIRED_LEVELS level (race 1: 100
  ... race 10: 290); their calibration is unchanged.
- Display: drag offers "Rival (127) VS You (120)"; center offers
  "<Rival> (140) VS You (120)", with laps and surface moved to the last
  line; the Mastery page's SPEED column is now RATING; level-up results
  show the new rating. Ratings display rounded, halves up.
- Tests: rating values, frozen drag rating across level-ups and saves,
  labels, and real-key checks of both panels.

Technical basis
- Keep game-specific code in this marked project. Adapt game_state.py,
  input_handler.py, and collision_manager.py and compose them from main.py.
- Use the documented gl_utils textured-rectangle records, load_texture,
  get_new_instances, to_gl/update_instances, and check_collision where needed.
- Use the atlas manifest's bottom-origin tile coordinates with the shared
  texture shader and its u_atlas_grid uniform.
- main.py composes world.py, car.py, game_state.py, input_handler.py, and
  collision_manager.py; player_save.py and world_save.py handle persistence. The active renderer
  batches instances by atlas and streams camera-relative records through the
  documented GL helpers.
